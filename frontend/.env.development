# Loaded only for `npm run dev` — overrides values from `.env` so the app uses your
# local Django API instead of https://api.mrdsphub.in/api
VITE_API_BASE_URL=http://localhost:9000/api
VITE_SITE_URL=http://localhost:5173
